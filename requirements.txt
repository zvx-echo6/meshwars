fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
aiolimiter==1.1.0
apscheduler==3.10.4
pygeohash==1.2.0
pydantic==2.9.2
pydantic-settings==2.5.2
sse-starlette==2.1.3
# Places Worth Going (docs/features/places.md): the >50%-of-a-cell park
# boundary test (app/places_seed.py) needs real polygon/multipolygon
# intersection, including parks with holes -- pure-Python clipping
# would be a lot of code to get right for that, and shapely's wheel
# bundles its own GEOS, so `pip install` is all a build needs (no apt
# packages, nothing beyond what the Dockerfile already runs).
shapely==2.1.2
# MQTT connector kind (app/mqtt_subscriber.py): reads Meshtastic text
# messages straight off a broker-agnostic MQTT broker -- there is no
# MQTT client in the stdlib, and paho-mqtt is the reference client the
# Meshtastic project itself documents for this integration.
paho-mqtt==2.1.0
# Same feature: Meshtastic's encrypted MQTT topics carry an AES-CTR
# ciphertext (channel-key encryption, not TLS -- see that module's
# docstring), and there is no AES implementation in the stdlib either.
# Deliberately NOT one of Meshtastic's own protobuf packages -- this
# app hand-rolls the handful of protobuf fields it actually needs (see
# app/mqtt_subscriber.py) rather than take on that dependency.
cryptography==49.0.0
# TOTP two-factor authentication (app/totp.py, app/totp_api.py):
# renders the otpauth:// enrollment URI as a scannable QR code. The
# TOTP math itself (RFC 6238, HMAC-SHA1) is pure stdlib -- no
# dependency needed for that -- but there is no QR encoder in the
# stdlib. segno is pure Python with zero dependencies of its own and
# emits SVG directly (see app/totp_api.py's _render_qr_svg()), so
# nothing runs client-side and nothing is fetched from a CDN to show
# the code -- the whole point, for a page rendering a shared secret.
segno==1.6.6
